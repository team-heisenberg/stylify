name: Push-to-EC2

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
      
    - name: Buid the application
      run: echo "Hello"

  Deploy:
    needs: build
    runs-on: ubuntu-latest
    
    steps:
      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh/
          echo "$PRIVATE_KEY" > ~/.ssh/staging.key
          chmod 600 ~/.ssh/staging.key
          cat >>~/.ssh/config <<END
          Host staging
            HostName ec2-35-155-214-56.us-west-2.compute.amazonaws.com
            User ec2-user
            IdentityFile ~/.ssh/staging.key
            StrictHostKeyChecking no
          END
        env:
          PRIVATE_KEY: ${{ secrets.EC2_SSH_KEY  }}

      - uses: actions/checkout@v2 
      - name: Deploy in EC2           
            
        run: |
          ssh ec2-user@ec2-35-155-214-56.us-west-2.compute.amazonaws.com '          

            cd ~/stylify &&
            git checkout main &&
            git fetch --all &&
            git reset --hard origin/main &&
            git pull origin main &&
          '
